import React,{useState} from "react";
import {API_URL} from "../config/api";
import "../Styles/CustomerTools.css";
export default function Security(){
 const [form,setForm]=useState({currentPassword:"",newPassword:"",confirmPassword:""});const [message,setMessage]=useState("");const [error,setError]=useState("");const [saving,setSaving]=useState(false);
 const submit=async e=>{e.preventDefault();setMessage("");setError("");if(form.newPassword!==form.confirmPassword)return setError("New passwords do not match.");if(form.newPassword.length<6)return setError("New password must be at least 6 characters.");setSaving(true);try{const r=await fetch(`${API_URL}/api/auth/password`,{method:"PUT",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({currentPassword:form.currentPassword,newPassword:form.newPassword})});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.message||"Password update failed.");setMessage("Password changed successfully.");setForm({currentPassword:"",newPassword:"",confirmPassword:""})}catch(e){setError(e.message)}finally{setSaving(false)}};
 return <main className="customer-tool-page"><section className="customer-tool-hero"><span>ACCOUNT SECURITY</span><h1>Login & Security</h1><p>Keep your Food App account protected with a strong password.</p></section><section className="customer-tool-content"><div className="security-layout"><form className="security-card" onSubmit={submit}><h2>Change password</h2><label>Current password<input type="password" value={form.currentPassword} onChange={e=>setForm({...form,currentPassword:e.target.value})} required/></label><label>New password<input type="password" value={form.newPassword} onChange={e=>setForm({...form,newPassword:e.target.value})} required minLength="6"/></label><label>Confirm new password<input type="password" value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})} required/></label>{error&&<div className="form-error">{error}</div>}{message&&<div className="form-success">{message}</div>}<button disabled={saving}>{saving?"Updating...":"Change Password"}</button></form><aside className="security-card"><h2>Security checklist</h2><p>✓ Password is never displayed in your profile.</p><p>✓ Authentication uses an HTTP-only login cookie.</p><p>✓ Passwords are stored using bcrypt hashing.</p><p>✓ Use a unique password that you do not reuse elsewhere.</p></aside></div></section></main>
}